# Python Dictionary vs Java HashMap
python_dict = {}
python_dict[key] = value
if key in python_dict: ...
python_defaultdict(int)[key] += 1
python_defaultdict(list)[key].append(val)
for key in dict.keys():
for item in dict.items():

# Java equivalent
Map<Object, Object> map = new HashMap<>();
map.put(key, value);
if (map.containsKey(key)) { ... }
map.put(key, map.getOrDefault(key, defaultValue: 0) + 1)
map.put(key).add(val)
for (Object key : map.keySet()) {}
for (Map.Entry<Object, Object> entry: map.entrySet()) {}

# Python List vs Java ArrayList/Array
python_list = []
python_list.append(value)
len(python_list)

# Java equivalent
List<Integer> list = new ArrayList<>(); or int[] array = new int[]{}
list.add(value);
list.size()

# Python Set vs Java HashSet
python_set = set()
python_set.add(value)
value in python_set

# Java equivalent
Set<Integer> set = new HashSet<>();
set.add(value);
set.contains(value)

# Python String Manipulation and Sorting
sorted(string, list) -> list()
key = "".join(sorted(string))

# Java equivalent
Arrays.sort() -> void
char[] key = string.toCharArray(); Arrays.sort(key); key = new String(key);

# Python Heapq
max_heap = []
for num, count in frequency.items():
    heapq.heappush(max_heap, (-count, num))
    heapq.heappop(max_heap)[index] (if tuples)

# Java PriorityQueue equivalent
    PriorityQueue<int[]> maxHeap = new PriorityQueue<>((a, b) -> b[1] - a[1]); (Comparator)
    for (Map.Entry<Integer, Integer> entry : frequency.entrySet()) {
        maxHeap.offer(new int[]{entry.getKey(), entry.getValue()});
        maxHeap.poll()[index] (if pairs)
    } 